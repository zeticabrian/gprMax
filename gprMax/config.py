# degree of the spline used to interpolate fields to points in the subgrid
# can be 1, 2, 3, 4
sg_interp_d = 3
# use the berenger cascading filter. Using the filter slightly reduces the
# accuracy of the simulation. However it is recommended as a large in increase
# in stability is observed.
filter = True
